/** User uploads & remote avatars — must exist in `next.config` `images.qualities`. */
export const IMAGE_QUALITY_PHOTO = 92;

/** Logos and crisp `/public` assets. */
export const IMAGE_QUALITY_SHARP = 95;
